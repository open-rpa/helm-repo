{{- $domain := print (required "domain required!" .Values.domain) "." (required "domainsuffix required!" .Values.domainsuffix) }}
{{- $apidomain := $domain }}
{{- if .Values.apidomain }}
  {{- $dummy := set . "apidomain" (print .Values.apidomain "." (required "domainsuffix required!" .Values.domainsuffix)) }}
{{- end }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/ingress.class: "traefik"
{{- if eq .Values.protocol "https" }}
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
  {{- if .Values.tlsSecret }}
    traefik.ingress.kubernetes.io/router.tls: "true"
  {{- end }}
  {{- if .Values.certresolver }}
    traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.certresolver | quote }}
  {{- end }}
  {{- if .Values.certmain }}
    traefik.ingress.kubernetes.io/router.tls.domains.0.main: {{ .Values.certmain | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
  {{- end }}
  {{- if .Values.certsans }}
    traefik.ingress.kubernetes.io/router.tls.domains.0.sans: {{ .Values.certsans | quote }}
  {{- end }}
{{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: web
{{- end }}
spec:
  rules:
    - host: "{{ $apidomain }}"
      http:
        paths:
          - path: /
            backend:
              serviceName: api
              servicePort: www
    - host: "mq.{{ $domain }}"
      http:
        paths:
          - path: /
            backend:
              serviceName: rabbitmq
              servicePort: rabbitmq-mgnt
{{- if .Values.openflow.license_key }}
    - host: "grafana.{{ $domain }}"
      http:
        paths:
          - path: /
            backend:
              serviceName: grafana
              servicePort: grafana
{{- end }}
{{- if and .Values.openflow.license_key (eq .Values.prometheus.enabled true) (eq .Values.prometheus.exposed true) }}
    - host: "prometheus.{{ $domain }}"
      http:
        paths:
          - path: /
            backend:
              serviceName: prometheus
              servicePort: prometheus
{{- end }}
{{- if and .Values.tlsSecret (eq .Values.protocol "https") }}
  tls:
    - secretName: {{ .Values.tlsSecret | quote }}
{{- end }}
---
{{ $ingress := (lookup "extensions/v1beta1" "Ingress" .Release.Namespace "useringress") }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: useringress
  annotations:
    kubernetes.io/ingress.class: traefik
{{- if eq .Values.protocol "https" }}
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
  {{- if .Values.tlsSecret }}
    traefik.ingress.kubernetes.io/router.tls: "true"
  {{- end }}
  {{- if .Values.certresolver }}
    traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.certresolver | quote }}
  {{- end }}
  {{- if .Values.certmain }}
    traefik.ingress.kubernetes.io/router.tls.domains.0.main: {{ .Values.certmain | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
  {{- end }}
  {{- if .Values.certsans }}
    traefik.ingress.kubernetes.io/router.tls.domains.0.sans: {{ .Values.certsans | quote }}
  {{- end }}
{{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: web
{{- end }}
spec:
  rules:
    - host: "random.{{ $domain }}"
      http:
        paths:
          - path: /
            backend:
              serviceName: api
              servicePort: www
{{ if $ingress }}
  {{- range $k, $v := (lookup "extensions/v1beta1" "Ingress" .Release.Namespace "useringress").spec.rules }}
    {{ if ne $k 0 }}
    - host: {{ $v.host }}
      http:
        paths:
        {{- range $k1, $v1 := $v.http.paths }}
          - path: {{ $v1.path }}
            backend:
              serviceName: {{$v1.backend.serviceName }} 
              servicePort: www
        {{- end }}
    {{ end }}
  {{ end }}
{{ end }}
{{- if .Values.tlsSecret }}
  tls:
    - secretName: {{ .Values.tlsSecret }}
{{- end }}
